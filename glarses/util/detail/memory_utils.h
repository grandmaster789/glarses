#ifndef GLARSES_UTIL_MEMORY_UTILS_H
#define GLARSES_UTIL_MEMORY_UTILS_H

namespace glarses::util {
    constexpr size_t memory_align(size_t size, size_t alignment);
}

#include "memory_utils.inl"

#endif
